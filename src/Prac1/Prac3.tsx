import React from "react";

const Prac3 = () => {
  // 중복되는 부분  number | string
  type sameType = number | string;

  let productID: sameType;
  let name: string;
  let price: sameType;
  let count: sameType;
  let total: number;
  const getTotal = (price: sameType, count: number) => {
    return total;
  };
  const setProduct = (productID: sameType, name: string, price: sameType) => {
    return productID;
  };

  return <div></div>;
};

export default Prac3;
